# Create the static library for the fem directory
# ===============================================
#add_library(ppm_mesh
#    mesh.cpp
#)

# Make headers accessible to other targets
# ========================================
#target_include_directories(ppm_mesh PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
